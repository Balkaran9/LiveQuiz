[phases.setup]
nixPkgs = ['dotnet-sdk_8']

[phases.install]
cmds = ['dotnet restore ITEC275LiveQuiz/ITEC275LiveQuiz.csproj']

[phases.build]
cmds = ['dotnet publish ITEC275LiveQuiz/ITEC275LiveQuiz.csproj -c Release -o out']

[start]
cmd = 'cd out && ./ITEC275LiveQuiz'
